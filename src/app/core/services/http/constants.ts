export enum apiEndPoint {
  getAllClients = '/api/clients/all',
  getByIdClient = '/api/clients/',
  login = '/api/auth/authenticate',
  //necesarios para products
  category = '/api/category/',
  vendor = '/api/vendor/',
  product = '/api/product/',
  // TODO: Add the missing endpoints
}
